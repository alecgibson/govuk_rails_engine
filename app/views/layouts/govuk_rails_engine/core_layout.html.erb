<%= render partial: 'layouts/govuk_rails_engine/base', locals: {
  hide_nav: true, # no breadcrumbs, you can include them using components if
                  # you need them, rather than using slimmer
  css_file: 'govuk_rails_engine/core-layout',
  js_file: 'govuk_rails_engine/header-footer-only'
} %>
